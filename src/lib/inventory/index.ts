/**
 * src/lib/inventory/index.ts
 *
 * Public API surface for the inventory module.
 * Import from here rather than individual files.
 *
 * Example (server-side only):
 *   import { createOrder, getStockInfo, adjustStock } from '@/lib/inventory'
 */

export * from './types'
export * from './operations'
export * from './orders'
